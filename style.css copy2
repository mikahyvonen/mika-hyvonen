:root {
  --yellow: #ffbe33;
  --black: #161314;
  --white: #fbf3ef;
  --blue: #0041cc;
  --border: none;
  --transition: 0.1s;
  --font: "DM sans", sans-serif;
}

hr {
  visibility: hidden;
}

html {
  scrollbar-width: thin;
  /*scrollbar-color: var(--yellow) yellow;*/
}

body {
  background-color: var(--black);
  margin: 0px;
  padding: 0px;
  overflow-y: scroll;
}

h2 {
  color: var(--white);
  font-family: var(--font);
  font-weight: 400;
  font-style: normal;
  padding: 0px;
  margin: 0px;
  font-size: 16pt;
  line-height: 150%;
}

p {
  color: var(--white);
  font-family: var(--font);
  font-weight: 400;
  font-size: 13pt;
  font-style: normal;
  transition-duration: var(--transition);
  line-height: 150%;
}

.footer {
  left: 0;
  bottom: 0;
  width: 100%;
  color: var(--white);
  text-align: center;
  font-family: "poppins", sans-serif;
  font-weight: 400;
  font-style: normal;
  transition-duration: var(--transition);
}

#linksHeader {
  /*background-color: #0B090A;*/
  border-style: var(--border);
  border-color: white;
  display: flex;
  justify-content: right;
  padding-top: 1vw;
  padding-bottom: 1vw;
  font-family: "poppins", sans-serif;
  font-weight: 400;
  font-style: normal;
  /*position: fixed;*/
  width: 90%;
  top: 0;
  left: 0;
  z-index: 1;
}

a {
  color: var(--white);
  padding:0px;
  margin-left: 0vw;
  margin-top:0px;
  margin-bottom: 0px;
  text-decoration: none;
  transition-duration: 0.1s;
}

.ingress {
  color: var(--yellow);
  margin-left: 0vw;
  margin-right: 0vw;
  transition-duration: 0.1s;
}

a:hover {
  color: var(--yellow);
  transition-duration: 0.1s;
}

.ingress: hover {
  color: var(--white) !important;
  transition-duration: 0.1s;
}

#logoHeader {
  border-style: var(--border);
  border-color: white;
  padding-top: 2.5vw;
  padding-left: 5vw;
  padding-right: 5vw;
  padding-bottom: 5vw;
  display: flex;
  flex-direction: column;
  transition-duration: var(--transition);
}

#ordbildLang {
  height: 200px;
  margin-bottom: 3vw;
  margin-top: 3vw;
  fill: var(--yellow);
}

.ordbild {
  fill: #ffffff;
  transition-duration: 0.3s;
  margin-top: 1.5vw;
  margin-bottom: 0.5vw;
}

@media screen and (max-width: 1100px) {
  .ordbild {
    width: 80vw;
  }
}

#star {
  height: 32vw;
  margin-left: 4vw;
  margin-right: 8vw;
  margin-top: -5vw;
  
}

#taglineHolder {
  display: flex;
  align-items: center;
  justify-content: center;
  transition-duration: var(--transition);
}

#tagline {
  width: 45vw;
  margin-top: 0.8vw;
}

.vimeo {
  width: 50vw;
  height: 30vw;
  transition-duration: var(--transition);
  /*pointer-events:none;*/
}

@media screen and (max-width: 1100px) {
  .vimeo {
    width:80vw;
    height: 50vw;
  }
}

.contentContainer {
  border-style: var(--border);
  border-color: white;
  width: 100%;
  padding-top: 1vw;
  padding-bottom: 1vw;
  display: flex;
  justify-content: center;
  align-content: center;
  gap: 5vw;
  margin-bottom: 6vw;
  transition-duration: var(--transition);
}

.contentContainer:hover {
  cursor: pointer;
}

@media screen and (max-width: 1100px) {
  .contentContainer {
    display: flex;
    flex-direction: column;
    justify-content: center;
    align-content: center;
  }
}

#EnKvallContainer:hover #EnKvallText {
  color: var(--yellow);
}

#EnKvallContainer:hover #EnKvallOrdbild {
  fill: var(--yellow);
}

#SkogenContainer:hover #SkogenText {
  color: var(--yellow);
}

#SkogenContainer:hover #SkogenOrdbild {
  fill: var(--yellow);
}

#FoolsGoldContainer:hover #FoolsGoldText {
  color: var(--yellow);
}

#FoolsGoldContainer:hover #FoolsGoldOrdbild {
  fill: var(--yellow);
}

#InteHelaContainer:hover #InteHelaText {
  color: var(--yellow);
}

#InteHelaContainer:hover #InteHelaOrdbild {
  fill: var(--yellow);
}

#FemKonversationerContainer:hover #FemKonversationerText {
  color: var(--yellow);
}

#FemKonversationerContainer:hover #FemKonversationerOrdbild {
  fill: var(--yellow);
}

.leftContainer {
  display: flex;
  flex-direction: column;
  align-content: center;
  align-items: center;
  gap: 10px;
  padding-left: 100px;
  transition-duration: var(--transition);
}

@media screen and (max-width: 1100px) {
  .leftContainer {
    padding-left:0px;
  }
}

#showreelVideo {
  width: 100%;
}

.textLeft {
  border-style: var(--border);
  border-color: red;
  width: 27.5vw;
  z-index: 1;
}

@media screen and (max-width: 1100px) {
  .textLeft {
    width: 80vw;
  }
}

.loopRight {
  height: 100%;
}

.homeVideo {
  height: 100%;
}

.ordBild {
  width: 25vw;
  margin-top: 3vw;
}

.rightContainer {
  display: flex;
  flex-direction: column;
  align-content: center;
  align-items: center;
  gap: 10px;
  padding-right: 100px;
}

@media screen and (max-width: 1100px) {
  .rightContainer {
    padding-right: 0px;
  }
}

.textRight {
  border-style: var(--border);
  border-color: red;
  width: 27.5vw;
}

@media screen and (max-width: 1100px) {
  .textRight {
    width: 80vw;
  }
}

.loopLeft {
  height: 100%;
}

















/*HERE STARTS SPECIFIC VIDEO SUBPAGE CSS*/
.videoPage {
  width: 75vw;
  height: 45vw;
  border-style: var(--border);
}

.SubpageContainer {
  display:flex;
  flex-direction: column;
  align-content: center;
  align-items: center;
}

.ordbildSubpage {
  fill: var(--white);
  width: 50vw;
  padding-top: 8vw;
  padding-bottom: 4vw;
}

.subpageText {
  border-style: var(--border);
}

.subpageImages {
  width: 40vw;
  margin-right: 6vw;
  border-style: var(--border);
  margin-bottom: 2vw;
  cursor: pointer;
}

.processContainer {
  border-style: var(--border);
  border-color: white;
  width: 80vw;
  padding-top: 1vw;
  padding-bottom: 1vw;
  display: flex;
  justify-content: center;
  align-content: center;
  gap: 5vw;
  margin-bottom: 8vw;
  transition-duration: var(--transition);
}

.tillbakaBox{
  padding-top: 1vw;
}
.tillbaka {
  font-family: var(--font);
  border-style: var(--border);
  border-color: white;
  justify-content: right;
  margin-left: 2vw;
  padding-top: 4vw;
  font-weight: 400;
  font-style: normal;
  width: 100%;
}

.modal {
  display: none; /* Hidden by default */
  position: fixed;
  z-index: 1000;
  left: 0;
  top: 0;
  width: 100%;
  height: 100%;
  background-color: rgba(0,0,0, 0.5); /* Semi-transparent background */
  justify-content: center;
  align-items: center;
}

.modal-content {
  max-width: 90%;
  max-height: 90%;
}